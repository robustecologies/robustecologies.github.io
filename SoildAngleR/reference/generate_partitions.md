# Generate all partitions of an integer into k parts

Generates all ways to write n as a sum of k non-negative integers.

## Usage

``` r
generate_partitions(n, k)
```

## Arguments

- n:

  The integer to partition

- k:

  The number of parts

## Value

A matrix where each row is a partition
