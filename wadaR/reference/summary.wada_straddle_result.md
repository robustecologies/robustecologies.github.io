# Summary method for wada_straddle_result objects

Provides extended statistics including saddle coverage, distance matrix
analysis, and diameter statistics.

## Usage

``` r
# S3 method for class 'wada_straddle_result'
summary(object, ...)
```

## Arguments

- object:

  A wada_straddle_result object.

- ...:

  Additional arguments (ignored).

## Value

A summary.wada_straddle_result object (invisibly).
