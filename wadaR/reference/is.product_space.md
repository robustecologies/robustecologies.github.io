# Check if an object is of class product_space

S3 method: apply [`is()`](https://rdrr.io/r/methods/is.html) to objects
of class `product_space`.

## Usage

``` r
is.product_space(x)
```

## Arguments

- x:

  An object to test.

## Value

Logical.

## Details

Implementation of `is.product_space`. Refer to the package vignettes and
the cited references for a complete algorithmic and mathematical
description.

## See also

[`plot.product_space`](https://robustecologies.github.io/lucifer/reference/plot.product_space.md),
[`print.product_space`](https://robustecologies.github.io/lucifer/reference/print.product_space.md),
[`summary.product_space`](https://robustecologies.github.io/lucifer/reference/summary.product_space.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving is.product_space
} # }
```
