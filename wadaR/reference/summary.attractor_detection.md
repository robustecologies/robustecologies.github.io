# Summary method for attractor_detection objects

Provides extended statistics including clustering confidence and
attractor characterization.

## Usage

``` r
# S3 method for class 'attractor_detection'
summary(object, ...)
```

## Arguments

- object:

  An attractor_detection object.

- ...:

  Additional arguments (ignored).

## Value

A summary.attractor_detection object (invisibly).
