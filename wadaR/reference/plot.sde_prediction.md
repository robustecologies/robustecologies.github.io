# Plot sde prediction

S3 method: apply
[`plot()`](https://rdrr.io/r/graphics/plot.default.html) to objects of
class `sde_prediction`.

## Usage

``` r
# S3 method for class 'sde_prediction'
plot(x, ...)
```

## Arguments

- x:

  See Details.

- ...:

  Additional arguments (currently unused).

## Value

See Details.

## Details

Implementation of `plot.sde_prediction`. Refer to the package vignettes
and the cited references for a complete algorithmic and mathematical
description.

## See also

[`print.sde_prediction`](https://robustecologies.github.io/lucifer/reference/print.sde_prediction.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving plot.sde_prediction
} # }
```
