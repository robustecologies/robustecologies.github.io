# Summary method for wada_grid_result objects

Provides extended statistics including the full M-value distribution,
convergence details, and boundary classification analysis.

## Usage

``` r
# S3 method for class 'wada_grid_result'
summary(object, ...)
```

## Arguments

- object:

  A wada_grid_result object.

- ...:

  Additional arguments (ignored).

## Value

A summary.wada_grid_result object (invisibly).
