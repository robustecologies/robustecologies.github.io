# Print method for summary.chameleon_surrogates objects

Print method for summary.chameleon_surrogates objects

## Usage

``` r
# S3 method for class 'summary.chameleon_surrogates'
print(x, digits = 4, ...)
```

## Arguments

- x:

  Object of class `summary.chameleon_surrogates`.

- digits:

  Number of significant digits to display (default 4).

- ...:

  Additional arguments (ignored).

## Value

The input `x`, returned invisibly.

## See also

[`summary.chameleon_surrogates`](https://robustecologies.github.io/chamaeleon/reference/summary.chameleon_surrogates.md).
