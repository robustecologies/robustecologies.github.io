# Summary model spec

S3 method: apply [`summary()`](https://rdrr.io/r/base/summary.html) to
objects of class `model_spec`.

## Usage

``` r
# S3 method for class 'model_spec'
summary(object, ...)
```

## Arguments

- object:

  See Details.

- ...:

  Additional arguments (currently unused).

## Value

See Details.

## Details

Implementation of `summary.model_spec`. Refer to the package vignettes
and the cited references for a complete algorithmic and mathematical
description.

## See also

[`model_spec`](https://robustecologies.github.io/lucifer/reference/model_spec.md),
[`code.model_spec`](https://robustecologies.github.io/lucifer/reference/code.md),
[`compile.model_spec`](https://robustecologies.github.io/lucifer/reference/compile.model_spec.md),
[`plot.model_spec`](https://robustecologies.github.io/lucifer/reference/plot.model_spec.md),
[`print.model_spec`](https://robustecologies.github.io/lucifer/reference/print.model_spec.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving summary.model_spec
} # }
```
