# Plot profile likelihood

S3 method: apply
[`plot()`](https://rdrr.io/r/graphics/plot.default.html) to objects of
class `profile_likelihood`.

## Usage

``` r
# S3 method for class 'profile_likelihood'
plot(x, col = NULL, ...)
```

## Arguments

- x:

  See Details.

- col:

  See Details.

- ...:

  Additional arguments (currently unused).

## Value

See Details.

## Details

Implementation of `plot.profile_likelihood`. Refer to the package
vignettes and the cited references for a complete algorithmic and
mathematical description.

## See also

[`profile_likelihood`](https://robustecologies.github.io/lucifer/reference/profile_likelihood.md),
[`print.profile_likelihood`](https://robustecologies.github.io/lucifer/reference/print.profile_likelihood.md),
[`summary.profile_likelihood`](https://robustecologies.github.io/lucifer/reference/summary.profile_likelihood.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving plot.profile_likelihood
} # }
```
