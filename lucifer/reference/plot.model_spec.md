# Plot model spec

S3 method: apply
[`plot()`](https://rdrr.io/r/graphics/plot.default.html) to objects of
class `model_spec`.

## Usage

``` r
# S3 method for class 'model_spec'
plot(x, ...)
```

## Arguments

- x:

  See Details.

- ...:

  Additional arguments (currently unused).

## Value

See Details.

## Details

Implementation of `plot.model_spec`. Refer to the package vignettes and
the cited references for a complete algorithmic and mathematical
description.

## See also

[`model_spec`](https://robustecologies.github.io/lucifer/reference/model_spec.md),
[`code.model_spec`](https://robustecologies.github.io/lucifer/reference/code.md),
[`compile.model_spec`](https://robustecologies.github.io/lucifer/reference/compile.model_spec.md),
[`print.model_spec`](https://robustecologies.github.io/lucifer/reference/print.model_spec.md),
[`summary.model_spec`](https://robustecologies.github.io/lucifer/reference/summary.model_spec.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving plot.model_spec
} # }
```
