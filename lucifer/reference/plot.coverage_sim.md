# Plot coverage sim

S3 method: apply
[`plot()`](https://rdrr.io/r/graphics/plot.default.html) to objects of
class `coverage_sim`.

## Usage

``` r
# S3 method for class 'coverage_sim'
plot(x, ...)
```

## Arguments

- x:

  See Details.

- ...:

  Additional arguments (currently unused).

## Value

See Details.

## Details

Implementation of `plot.coverage_sim`. Refer to the package vignettes
and the cited references for a complete algorithmic and mathematical
description.

## See also

[`coverage_sim`](https://robustecologies.github.io/lucifer/reference/coverage_sim.md),
[`print.coverage_sim`](https://robustecologies.github.io/lucifer/reference/print.coverage_sim.md),
[`summary.coverage_sim`](https://robustecologies.github.io/lucifer/reference/summary.coverage_sim.md).

## Examples

``` r
if (FALSE) { # \dontrun{
## see package vignettes for full examples involving plot.coverage_sim
} # }
```
